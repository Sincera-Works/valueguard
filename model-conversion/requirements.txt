# Pinned for reproducibility. Tested on Apple Silicon, Python 3.11.
# coremltools requires Python < 3.13 as of mid-2026.

torch==2.5.1
transformers==4.47.1
coremltools==8.1
pillow==11.0.0
numpy==1.26.4

# For embed_captions.py — only needs the text tower
sentencepiece==0.2.0
protobuf==5.29.1

# For export_onnx.py / test_onnx_parity.py — the Windows-port model path
onnx==1.21.0
onnxruntime==1.26.0
